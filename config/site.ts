import { SiteConfig, ContactConfig } from "@/types"

export const siteConfig: SiteConfig = {
  name: "Cass",
  author: "cass",
  description:
    "Cass Web-Developer",
}

export const contactConfig: ContactConfig = {
  email: "cassmatheuss@gmail.com",
}
